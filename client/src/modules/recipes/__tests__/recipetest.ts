/* Example
descripe('if the image hasnt been favourited', async () => {
   const { findByTest } = readerComponent();
   const notFavouritedText = await findByText('Not Favourited);
   expect(notFavouritedText).toBeInTheDocument();
});


*/
